#include "main.h"

/**
 * _abs - Computes absolute value of an integer
 * @n: Character to be checked
 *
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
