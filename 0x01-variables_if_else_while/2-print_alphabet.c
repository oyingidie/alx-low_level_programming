#include <stdio.h>

/**
 * main - Display inputted condition
 *
 * Return: Always 0 (SUCCESS)
 */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
		putchar('\n');
		return (0);
}
